'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { FileText, Search, Upload, MessageSquare, MoreHorizontal, Sparkles, Clock3, FileUp, ChevronRight, Menu, X, Plus, CheckCircle2 } from 'lucide-react'

type Document = { id: string; name: string; type: string; size: string; updated: string; color: string; status?: string }

const documents: Document[] = [
  { id: 'research-paper', name: 'Research Paper.pdf', type: 'PDF', size: '2.4 MB', updated: 'Updated 2 hours ago', color: 'rose' },
  { id: 'project-proposal', name: 'Project Proposal.docx', type: 'DOCX', size: '890 KB', updated: 'Updated yesterday', color: 'blue' },
  { id: 'meeting-notes', name: 'Meeting Notes.txt', type: 'TXT', size: '42 KB', updated: 'Updated 3 days ago', color: 'amber' },
  { id: 'product-spec', name: 'Product Specification.pdf', type: 'PDF', size: '1.8 MB', updated: 'Updated 5 days ago', color: 'rose' },
]

function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) {
  return <>
    {open && <button aria-label="Close menu" onClick={onClose} className="fixed inset-0 z-30 bg-slate-950/20 lg:hidden" />}
    <aside className={`fixed inset-y-0 left-0 z-40 flex w-[260px] flex-col border-r border-slate-200 bg-white transition-transform lg:static lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}>
      <div className="flex h-16 items-center justify-between border-b border-slate-100 px-5">
        <Link href="/" className="flex items-center gap-2.5 text-lg font-bold tracking-tight text-slate-900"><span className="grid size-8 place-items-center rounded-lg bg-indigo-600 text-white"><FileText className="size-[18px]" /></span>DocMind<span className="text-indigo-600">AI</span></Link>
        <button className="lg:hidden" onClick={onClose} aria-label="Close navigation"><X className="size-5 text-slate-500" /></button>
      </div>
      <nav className="flex flex-col gap-1 p-4 text-sm font-medium">
        <Link className="flex items-center gap-3 rounded-lg bg-indigo-50 px-3 py-2.5 text-indigo-700" href="/"><FileText className="size-[18px]" />My Documents</Link>
        <Link className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-slate-600 hover:bg-slate-50" href="/"><MessageSquare className="size-[18px]" />Recent Chats</Link>
      </nav>
      <div className="mt-auto border-t border-slate-100 p-4"><div className="rounded-xl bg-slate-50 p-3"><div className="flex items-center gap-2 text-xs font-semibold text-slate-700"><Sparkles className="size-4 text-indigo-600" />Free plan</div><p className="mt-2 text-xs leading-5 text-slate-500">3 of 5 documents used</p><div className="mt-2 h-1.5 rounded-full bg-slate-200"><div className="h-full w-3/5 rounded-full bg-indigo-600" /></div><button className="mt-3 w-full rounded-md bg-indigo-600 py-2 text-xs font-semibold text-white hover:bg-indigo-700">Upgrade plan</button></div><div className="mt-4 flex items-center gap-3 px-1"><div className="grid size-8 place-items-center rounded-full bg-indigo-100 text-xs font-bold text-indigo-700">JD</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-slate-800">Jordan Davis</p><p className="truncate text-[11px] text-slate-500">jordan@example.com</p></div><MoreHorizontal className="size-4 text-slate-400" /></div></div>
    </aside>
  </>
}

function DocumentCard({ document }: { document: Document }) {
  const iconClass = document.color === 'rose' ? 'bg-rose-50 text-rose-500' : document.color === 'blue' ? 'bg-blue-50 text-blue-500' : 'bg-amber-50 text-amber-500'
  return <Link href={`/documents/${document.id}`} className="group flex flex-col justify-between rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:border-indigo-200 hover:shadow-md"><div><div className="flex items-start justify-between"><div className={`grid size-10 place-items-center rounded-lg ${iconClass}`}><FileText className="size-5" /></div><button aria-label={`More options for ${document.name}`} onClick={(e) => e.preventDefault()} className="rounded-md p-1 text-slate-400 hover:bg-slate-50 hover:text-slate-700"><MoreHorizontal className="size-5" /></button></div><h3 className="mt-4 truncate text-sm font-semibold text-slate-900">{document.name}</h3><p className="mt-1 text-xs text-slate-500">{document.type} · {document.size}</p></div><p className="mt-6 flex items-center gap-1.5 text-xs text-slate-400"><Clock3 className="size-3.5" />{document.updated}</p></Link>
}

export default function Page() {
  const [menuOpen, setMenuOpen] = useState(false)
  const [query, setQuery] = useState('')
  const [uploaded, setUploaded] = useState(false)
  const filtered = useMemo(() => documents.filter((doc) => doc.name.toLowerCase().includes(query.toLowerCase())), [query])
  return <div className="min-h-screen bg-slate-50 text-slate-900"><div className="flex min-h-screen"><Sidebar open={menuOpen} onClose={() => setMenuOpen(false)} /><main className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b border-slate-200 bg-white px-5 sm:px-8"><button className="rounded-lg p-2 hover:bg-slate-50 lg:hidden" onClick={() => setMenuOpen(true)} aria-label="Open navigation"><Menu className="size-5" /></button><div className="hidden text-sm text-slate-500 lg:block">Workspace / <span className="font-medium text-slate-900">My Documents</span></div><div className="ml-auto flex items-center gap-4"><button className="relative rounded-lg p-2 text-slate-500 hover:bg-slate-50" aria-label="Notifications"><span className="absolute right-2 top-1 size-1.5 rounded-full bg-indigo-600" /><svg className="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4" /></svg></button><div className="hidden size-8 place-items-center rounded-full bg-indigo-100 text-xs font-bold text-indigo-700 sm:grid">JD</div></div></header><div className="mx-auto max-w-6xl px-5 py-8 sm:px-8 lg:py-10"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-medium text-indigo-600">Your knowledge workspace</p><h1 className="text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">My Documents</h1><p className="mt-2 text-sm text-slate-500">Upload documents and chat with your files using AI.</p></div><button onClick={() => document.getElementById('file-upload')?.click()} className="inline-flex items-center justify-center gap-2 rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700"><Upload className="size-4" />Upload document</button></div><div className="mt-8 rounded-xl border border-indigo-100 bg-indigo-50/70 p-4 sm:p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="grid size-11 shrink-0 place-items-center rounded-xl bg-white text-indigo-600 shadow-sm"><FileUp className="size-5" /></div><div className="flex-1"><h2 className="text-sm font-semibold text-slate-900">Add a document to get started</h2><p className="mt-1 text-xs leading-5 text-slate-500">Upload a PDF, DOCX, or TXT file. DocMind will analyze it so you can ask questions.</p></div><label htmlFor="file-upload" className="inline-flex cursor-pointer items-center justify-center gap-2 rounded-lg border border-indigo-200 bg-white px-3.5 py-2 text-xs font-semibold text-indigo-700 hover:bg-indigo-50"><Upload className="size-4" />Choose file<input id="file-upload" className="sr-only" type="file" accept=".pdf,.docx,.txt" onChange={() => setUploaded(true)} /></label></div>{uploaded && <div className="mt-3 flex items-center gap-2 text-xs font-medium text-emerald-700"><CheckCircle2 className="size-4" />Document selected. Upload processing will begin shortly.</div>}</div><div className="mt-10 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-semibold text-slate-950">Your library <span className="ml-1 text-sm font-normal text-slate-400">{documents.length}</span></h2></div><div className="flex gap-2"><label className="relative block flex-1 sm:w-64"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search documents..." className="h-9 w-full rounded-lg border border-slate-200 bg-white pl-9 pr-3 text-xs outline-none ring-indigo-500 placeholder:text-slate-400 focus:ring-2" /></label><button className="flex size-9 items-center justify-center rounded-lg border border-slate-200 bg-white text-slate-500 hover:bg-slate-50" aria-label="Add document"><Plus className="size-4" /></button></div></div><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{filtered.map((document) => <DocumentCard key={document.id} document={document} />)}<button onClick={() => document.getElementById('file-upload')?.click()} className="flex min-h-[178px] flex-col items-center justify-center rounded-xl border border-dashed border-slate-300 bg-white/60 p-4 text-center transition hover:border-indigo-300 hover:bg-indigo-50/30"><span className="grid size-10 place-items-center rounded-full bg-indigo-50 text-indigo-600"><Plus className="size-5" /></span><span className="mt-3 text-sm font-semibold text-slate-700">Add document</span><span className="mt-1 text-xs text-slate-400">PDF, DOCX, or TXT</span></button></div>{filtered.length === 0 && <div className="py-16 text-center text-sm text-slate-500">No documents match “{query}”.</div>}<div className="mt-12 flex items-center justify-between border-t border-slate-200 pt-5 text-xs text-slate-400"><span>DocMind AI · Secure document intelligence</span><Link href="/" className="hover:text-indigo-600">Help center <ChevronRight className="ml-1 inline size-3" /></Link></div></div></main></div></div>
}
